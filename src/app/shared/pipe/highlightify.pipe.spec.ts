import { HighlightifyPipe } from './highlightify.pipe';

describe('HighlightifyPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightifyPipe();
    expect(pipe).toBeTruthy();
  });
});
